export const selectIsModalOpen = (state) => state.modal.isOpen;
export const selectIsAddModalOpen = (state) => state.modal.isAddOpen;
export const selectIsEditModalOpen = (state) => state.modal.isEditOpen;
export const selectIsDeleteModalOpen = (state) => state.modal.isDeleteOpen;
